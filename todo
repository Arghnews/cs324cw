-- make sat work with rotations - requires writing a function to return vertices of shape
taking into account rotation, ie. will need to move them
-- think need to take cross product of all edges as well as face normals?
-- try crossing all the face normals
